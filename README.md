# Simulated Annealing for MAX-SAT


## Requirements

- **Python** ≥ 3.10
- Recommended packages:
  - `numpy` (for arrays and vectorised operations, if used)

Install dependencies:
- `pip install -r requirements.txt`

or manually:
- `pip install numpy pandas matplotlib`

